package com.pizzaOrdering.model;


//this is enum in which roles are enumType
public enum Role {

	CUSTOMER, DELIVERYPARTNER, ADMIN, MANAGER, USER;
	//USER role going to access all the roles when we want.
}

